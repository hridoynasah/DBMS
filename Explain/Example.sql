CREATE DATABASE IF NOT EXISTS management;
# IF NOT EXISTS -> Clause : same name er database already exists korle error theke prevent kore.

USE management;
# Switches the active database to management
# jate kore sob command ei database er upor apply hoy

CREATE TABLE Books(
    ISBN 
)